{
  "name": "quick-actions-launcher",
  "version": "1.0.0",
  "private": true,
  "description": "Firefox extension: Spotlight-style command palette for tabs, bookmarks, and history.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.1",
    "eslint": "^9.27.0",
    "globals": "^16.1.0",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.1"
  }
}
